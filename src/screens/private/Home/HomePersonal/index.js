import React, { useState } from 'react'
import { Button, Input, Text } from '@rneui/themed';
import { View } from 'react-native';

const HomePersonal = () => { 
  return (
    <View style={{ flex: 1 }}>
      <Text> Painel Personal</Text>
    </View>
  );
}
export default HomePersonal;